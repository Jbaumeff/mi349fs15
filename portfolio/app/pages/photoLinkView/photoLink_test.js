'use strict';

describe('myApp.photoLink module', function() {

  beforeEach(module('myApp.photoLink'));

  describe('photoLink controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var photoLinkCtrl = $controller('PhotoLinkCtrl');
      expect(photoLinkCtrl).toBeDefined();
    }));

  });
});
